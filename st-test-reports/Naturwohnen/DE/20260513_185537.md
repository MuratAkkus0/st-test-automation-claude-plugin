# Sales Tracking Test Report — Naturwohnen (DE)

| | |
|---|---|
| **Partner** | Naturwohnen |
| **Market** | DE (Germany) |
| **Portal** | https://www.moebel.de |
| **Test date** | 2026-05-13 18:55 (local) |
| **Tester** | Murat Akkus |
| **Overall status** | ⚠️ PARTIAL — Base Part WORKING, Conversion Part not tested |
| **Stop reason** | Tester explicitly requested the run to stop at checkout review without submitting an order. |

---

## Phase 1 — Navigation & moeclid Capture

| | |
|---|---|
| Listing path tried | `/shops?ps=asc` (price-sorted) |
| Listing pass that found the partner | `shops` (first pass — no fallback to `/marken` needed) |
| Partner shop URL | https://www.moebel.de/shops/naturwohnen |
| `partnerId` in redirect | `7a4d28ce-ac02-4b0a-80ad-eb29699b9223` |
| `partnerName` in redirect | `Naturwohnen` |
| Cheapest product selected | Regalboden "Arbor" schmal Erle natur geölt (39,95 €) |
| Final URL after redirect chain | `https://www.naturwohnen.de/moebel/regale/arbor-regalboden-schmal_560050_22763/?ReferrerID=13.00&moeclid=b08f43da-8a7b-4b4f-b1a1-a28ae736830e` |
| `moeclid` present in final URL | ✅ Yes |
| `moeclid` value | `b08f43da-8a7b-4b4f-b1a1-a28ae736830e` |
| Verdict | ✅ PASS |

---

## Phase 2 — Base Part Verification

### Initial page state

| | |
|---|---|
| Partner domain | `www.naturwohnen.de` |
| Shop platform | PlentyMarkets |
| Overlays present | None visible. Promotional bar (–20% NATURMÖBEL-WOCHE) at top, two `×` close buttons for newsletter / promo modals were dismissed before storage check. |

### Cookie consent

| | |
|---|---|
| Banner present on this visit | ❌ No |
| Consent platform | First-party PlentyMarkets consent layer (no third-party CMP — no Cookiebot / Usercentrics / OneTrust / Consentmanager / Klaro / __tcfapi globals were detected) |
| Banner type | n/a (no banner shown) |
| Action taken | None — consent state was already persisted in the `plenty-shop-cookie` cookie from a prior session (all categories granted: necessary, tracking, media, paypal, convenience). Phase 0 cleared portal state on `moebel.de` only; the partner domain `naturwohnen.de` was not known until after the redirect chain. |
| Caveat | Because consent was pre-existing, this was effectively a returning-visitor scenario rather than a true first-visit. The Base Tag fired in a fully-consented context, which is what we expect to see for a real returning visitor. |

### Newsletter / popup handling

| | |
|---|---|
| Popup present | Two close-button overlays in the initial snapshot |
| Action taken | Dismissed before storage check |

### Storage check

| | |
|---|---|
| `moeclid` cookie present | ❌ No (rules out Server-Side integration) |
| `localStorage.MOEBEL_CLICKOUT_ID` present | ✅ Yes |
| `localStorage` raw value | `{"date":"2026-05-13T16:34:11.727Z","clickId":"b08f43da-8a7b-4b4f-b1a1-a28ae736830e"}` |
| Stored `clickId` matches URL `moeclid` | ✅ Yes (`b08f43da-8a7b-4b4f-b1a1-a28ae736830e`) |
| All cookies on the page | `_ga`, `plenty-shop-cookie`, `_fbp`, `_pin_unauth`, `__stripe_mid`, `_gcl_au`, `XSRF-TOKEN`, `_ga_57F7GBCD0V` |

### Verdict

| | |
|---|---|
| Base Part status | ✅ WORKING |
| Detected integration type | **Client-Side (localStorage)** — likely GTM Client-Side, Manual Client-Side, or a Shopify-style pixel adapted for PlentyMarkets |
| Notes | The Base Tag fires correctly on `page_view` and writes the `MOEBEL_CLICKOUT_ID` JSON blob to `localStorage`. The date stamp inside the value (`2026-05-13T16:34:11.727Z`) is recent and within the test window, confirming the tag fires on every page load (not just a stale value from earlier). |

---

## Phase 3 — Purchase Flow (stopped at checkout review by tester instruction)

| | |
|---|---|
| Item added | Regalboden "Arbor" schmal Erle natur geölt (39,95 €) |
| Cart subtotal | 39,95 € |
| Shipping (DPD – Paket Möbel) | 29,95 € |
| Grand total shown | 69,90 € |
| Checkout path taken | Guest checkout (`Als Gast bestellen`) — email `partner@moebel.de` not yet registered on this shop, so no login was required |
| Billing/shipping address used | Mia Moebel.de-Test, Moebel.de Str. 3, 20095 Hamburg, Deutschland (primary DE test identity from `st-market-reference`) |
| Address validation | Accepted on first try — no fallback address (Reeperbahn 20359) needed |
| Payment method pre-selected | Vorkasse |
| Order submitted | ❌ No — tester explicitly requested the run to stop at the checkout review page before clicking `Kaufen`. |
| Order number | — (no order placed) |

---

## Phase 4 — Conversion Part Verification (not performed)

The Conversion Part could not be verified on this run because no order was submitted. As a result:

- The Performance API was not inspected for the sale call.
- `MOEBEL_SALES`, `PARTNER_KEY`, `MARKET`, and the stored `clickId` at the order confirmation page were not captured.
- The payload of the `sales` request to `redirect.moebel.de/api/1.0/moebel/de/sales` was not validated.
- Console errors at the confirmation page were not captured (the page was never reached).

### PARTNER_KEY captured by the test

| | |
|---|---|
| PARTNER_KEY used in sale call | — (not captured — Conversion Part not reached) |
| MARKET captured in sale call | — (not captured — Conversion Part not reached) |
| PARTNER_KEY verification | Skipped — Conversion Part not reached, and no expected key was provided by the tester. |

To verify the Conversion Part, run the test again without the stop-before-order instruction. The Base Part result from this run will still apply.

---

## Phases status summary

| Phase | Status |
|---|---|
| Phase 0 — Pre-test setup (portal CMP) | ✅ Done |
| Phase 1 — Navigation + moeclid capture | ✅ PASS |
| Phase 2 — Base Part verification | ✅ WORKING (Client-Side / localStorage) |
| Phase 3 — Purchase flow | ⚠️ Partial — stopped at checkout review |
| Phase 4 — Conversion Part verification | ⏭️ Skipped — no order submitted |
| Phase 5 — Console error check (confirmation page) | ⏭️ Skipped — confirmation page not reached |

---

## Root Cause Analysis

No failures observed in the phases that were executed. The Base Tag fires correctly after the redirect-chain and writes the moeclid to `localStorage`. The only open item is verifying the Conversion Tag — which requires a real order on the partner's shop.

---

## Recommendations

- **Confirm the integration type with the partner.** The moeclid is stored in `localStorage.MOEBEL_CLICKOUT_ID`, which is consistent with the [Client-Side Tracking with Google Tag Manager](https://partner-integration.moebel.de/sales-tracking/1/google-tag-manager-client-side.html) or [Manual Client-Side Integration](https://partner-integration.moebel.de/sales-tracking/1/manual-client-side-integration.html) patterns. Whether the partner uses GTM or a hand-rolled snippet should be verified before any troubleshooting is required.
- **Re-run the test to completion when ready to verify the Conversion Tag.** A second run that submits the order will exercise the call to `https://redirect.moebel.de/api/1.0/moebel/de/sales` and let us capture the partner's `PARTNER_KEY` for cross-checking against ACM.

---

## Test Data References

| | |
|---|---|
| Captured moeclid | `b08f43da-8a7b-4b4f-b1a1-a28ae736830e` |
| moebel.de `partnerId` (from redirect) | `7a4d28ce-ac02-4b0a-80ad-eb29699b9223` |
| Captured PARTNER_KEY (from Conversion Part) | — (Conversion Part not run) |
| Test order number | — (no order placed) |
| Sales API endpoint for DE | `https://redirect.moebel.de/api/1.0/moebel/de/sales` |

---

*Created by Sales Tracking Test Automation*
